{
  "scripts": {
    "start": "parcel ./src/*.html --no-cache --no-source-maps --dist-dir dist",
    "build": "rm -rf dist && parcel build ./src/*.html --no-cache --no-source-maps --dist-dir dist"
  },
  "dependencies": {
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "json-server": "^0.17.3",
    "parcel": "^2.9.3"
  }
}
